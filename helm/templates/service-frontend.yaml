apiVersion: v1
kind: Service
metadata:
  name: kusho-frontend-service
spec:
  selector:
    app: kusho-frontend
  ports:
    - protocol: TCP
      port: 80     # The service port
      targetPort: 80  # The container's port in the Pod
  type: ClusterIP
